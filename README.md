# Homebrew Tap for Thinkwork

Install the Thinkwork CLI via Homebrew:

```bash
brew install thinkwork-ai/tap/thinkwork
```

This tap is automatically updated by the [release workflow](https://github.com/thinkwork-ai/thinkwork/blob/main/.github/workflows/release.yml) when a new version is tagged.
